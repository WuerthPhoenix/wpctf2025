# Monitoring Dashboard

We gained the credentials to a monitoring dashboard with a list of hosts. Unfortunately, the relevant host is part of a different tenant that we cannot access directly. Let's see if we can find a way to access it.

Credentials:

username: `Peter `

password: `peter_120867`
